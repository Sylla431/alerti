# Dépendances légères pour Vercel (< 500 Mo).
# Pas de TensorFlow, OpenCV, rasterio, netCDF — trop volumineux.
# Prédictions LSTM : déployer sur Railway (voir VERCEL_DEPLOY.md).

Flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0
